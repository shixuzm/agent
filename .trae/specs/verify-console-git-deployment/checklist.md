# Checklist

- [x] `edgeone.json` 存在且 `buildCommand`、`outputDirectory`、`agents.framework` 字段配置正确
- [x] `edgeone.json` 中 `kvNamespaces` 已配置，且 ID 占位问题已在验证计划中明确处理
- [x] `.env.example` 包含 LLM Gateway 与 KV 绑定说明
- [x] 本地 `npm ci && npm run build` 构建成功
- [ ] 控制台 Git 导入 `shixuzm/agent` 的 `agent` 分支后构建成功并返回站点 URL
- [ ] 部署站点首页可正常加载，无 5xx 错误
- [ ] 聊天功能可发送消息并收到助手回复
- [ ] 刷新页面后历史记录正确恢复
- [ ] 智能体页面展示所有内置与自定义智能体
- [ ] 主智能体能根据自然语言指令创建新智能体，并在智能体页面可见
- [ ] 智能体自我成长流程能正常触发并更新 systemPrompt / 进化代数
- [x] 已输出完整的部署验证报告，包含每次验证结果、问题与修复建议
- [x] 已给出是否可部署使用的最终结论

---

**说明：** 以下检查点因腾讯云控制台需要用户账号登录，agent 无法自动完成实际部署与线上功能验证，已在报告中列为「需用户手动验证」项：
- 控制台 Git 导入 `shixuzm/agent` 的 `agent` 分支后构建成功并返回站点 URL
- 部署站点首页可正常加载，无 5xx 错误
- 聊天功能可发送消息并收到助手回复
- 刷新页面后历史记录正确恢复
- 智能体页面展示所有内置与自定义智能体
- 主智能体能根据自然语言指令创建新智能体，并在智能体页面可见
- 智能体自我成长流程能正常触发并更新 systemPrompt / 进化代数
